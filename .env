# Leave VITE_API_BASE_URL unset to use same-origin proxy (Netlify/Vite) — fixes CORS without backend deploy.
# For direct backend URL (needs backend CORS configured):
# VITE_API_BASE_URL=https://coffeeshop-backend-production.up.railway.app
# VITE_API_BASE_URL=http://localhost:53691
